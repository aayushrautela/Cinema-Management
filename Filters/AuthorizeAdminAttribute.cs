using Microsoft.AspNetCore.Authorization;

namespace CinemaTicketSystemCore.Filters
{
    // In ASP.NET Core, we use the built-in [Authorize(Roles = "Admin")] attribute
    // This file is kept for compatibility but the attribute is not needed
    // Controllers should use [Authorize(Roles = "Admin")] directly
}

